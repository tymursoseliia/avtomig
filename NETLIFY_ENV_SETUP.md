# Настройка переменных окружения в Netlify

После деплоя сайта на Netlify, нужно добавить переменные окружения для работы Telegram бота и Supabase.

## 📋 Шаги настройки:

### 1. Откройте настройки сайта на Netlify

1. Перейдите на [app.netlify.com](https://app.netlify.com/)
2. Выберите ваш сайт из списка
3. Нажмите **"Site settings"** (Настройки сайта)
4. В боковом меню выберите **"Environment variables"** (Переменные окружения)

### 2. Добавьте переменные окружения

Нажмите **"Add a variable"** и добавьте следующие переменные одну за другой:

#### Supabase переменные:

**Переменная 1:**
```
Key: NEXT_PUBLIC_SUPABASE_URL
Value: https://gnsnuyqzbwgssfkgxcpc.supabase.co
```

**Переменная 2:**
```
Key: NEXT_PUBLIC_SUPABASE_ANON_KEY
Value: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imduc251eXF6Yndnc3Nma2d4Y3BjIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA4NzU3ODksImV4cCI6MjA4NjQ1MTc4OX0.D7u9_iWU6qEaQzoGNUov_akfVsc3nTf-N50TcWWEeZY
```

#### Telegram Bot переменные:

**Переменная 3:**
```
Key: TELEGRAM_BOT_TOKEN
Value: 8509211479:AAFY-3uNXTdFafiQmBF5wGOq1RlVexsWVY0
```

**Переменная 4:**
```
Key: TELEGRAM_CHAT_ID
Value: 8296095050
```

### 3. Сохраните и передеплойте

1. После добавления всех переменных нажмите **"Save"**
2. Перейдите в **"Deploys"**
3. Нажмите **"Trigger deploy"** → **"Deploy site"**
4. Дождитесь завершения деплоя

## ✅ Готово!

Теперь ваш сайт на Netlify будет:
- ✅ Работать с базой данных Supabase
- ✅ Отправлять заявки в Telegram
- ✅ Сохранять данные об автомобилях и видео-отзывах

## 🔒 Безопасность

⚠️ **ВАЖНО:** Никогда не коммитьте файл `.env.local` в Git!
- Файл `.env.local` уже добавлен в `.gitignore`
- Все секретные токены хранятся только в Netlify и локально
- Переменные с префиксом `NEXT_PUBLIC_` видны в браузере (это нормально для Supabase URL)

## 🧪 Проверка работы

После деплоя проверьте:
1. Откройте ваш сайт на Netlify
2. Заполните форму заявки
3. Проверьте Telegram - должно прийти сообщение!

Если сообщения не приходят, проверьте:
- Правильно ли скопированы все переменные
- Передеплоили ли сайт после добавления переменных
- Нет ли ошибок в логах деплоя (раздел "Deploy logs")
